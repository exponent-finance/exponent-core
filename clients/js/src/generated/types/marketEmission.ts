/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";
import {
  getNumberDecoder,
  getNumberEncoder,
  type Number,
  type NumberArgs,
} from ".";

export type MarketEmission = {
  /**
   * Escrow account that receives the emissions from the SY program
   * And then passes them through to the user
   */
  tokenEscrow: Address;
  /** Index for converting LP shares into earned emissions */
  lpShareIndex: Number;
  /** The difference between the staged amount and collected emission amount */
  lastSeenStaged: bigint;
};

export type MarketEmissionArgs = {
  /**
   * Escrow account that receives the emissions from the SY program
   * And then passes them through to the user
   */
  tokenEscrow: Address;
  /** Index for converting LP shares into earned emissions */
  lpShareIndex: NumberArgs;
  /** The difference between the staged amount and collected emission amount */
  lastSeenStaged: number | bigint;
};

export function getMarketEmissionEncoder(): FixedSizeEncoder<MarketEmissionArgs> {
  return getStructEncoder([
    ["tokenEscrow", getAddressEncoder()],
    ["lpShareIndex", getNumberEncoder()],
    ["lastSeenStaged", getU64Encoder()],
  ]);
}

export function getMarketEmissionDecoder(): FixedSizeDecoder<MarketEmission> {
  return getStructDecoder([
    ["tokenEscrow", getAddressDecoder()],
    ["lpShareIndex", getNumberDecoder()],
    ["lastSeenStaged", getU64Decoder()],
  ]);
}

export function getMarketEmissionCodec(): FixedSizeCodec<
  MarketEmissionArgs,
  MarketEmission
> {
  return combineCodec(getMarketEmissionEncoder(), getMarketEmissionDecoder());
}
