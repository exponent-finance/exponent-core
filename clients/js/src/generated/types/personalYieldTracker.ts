/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";
import {
  getNumberDecoder,
  getNumberEncoder,
  type Number,
  type NumberArgs,
} from ".";

/** Generic tracker for interest and emissions earned by deposits */
export type PersonalYieldTracker = {
  /**
   * The index is the per-share value of the SY token
   * Note that the YT balance must be converted to the equivalent SY balance
   */
  lastSeenIndex: Number;
  /** Staged tokens that may be withdrawn */
  staged: bigint;
};

export type PersonalYieldTrackerArgs = {
  /**
   * The index is the per-share value of the SY token
   * Note that the YT balance must be converted to the equivalent SY balance
   */
  lastSeenIndex: NumberArgs;
  /** Staged tokens that may be withdrawn */
  staged: number | bigint;
};

export function getPersonalYieldTrackerEncoder(): FixedSizeEncoder<PersonalYieldTrackerArgs> {
  return getStructEncoder([
    ["lastSeenIndex", getNumberEncoder()],
    ["staged", getU64Encoder()],
  ]);
}

export function getPersonalYieldTrackerDecoder(): FixedSizeDecoder<PersonalYieldTracker> {
  return getStructDecoder([
    ["lastSeenIndex", getNumberDecoder()],
    ["staged", getU64Decoder()],
  ]);
}

export function getPersonalYieldTrackerCodec(): FixedSizeCodec<
  PersonalYieldTrackerArgs,
  PersonalYieldTracker
> {
  return combineCodec(
    getPersonalYieldTrackerEncoder(),
    getPersonalYieldTrackerDecoder(),
  );
}
