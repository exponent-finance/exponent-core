/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { EXPONENT_CORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const REALLOC_MARKET_DISCRIMINATOR = new Uint8Array([40]);

export function getReallocMarketDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 1).encode(
    REALLOC_MARKET_DISCRIMINATOR,
  );
}

export type ReallocMarketInstruction<
  TProgram extends string = typeof EXPONENT_CORE_PROGRAM_ADDRESS,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountAdminState extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountRent extends string | AccountMeta<string> =
    "SysvarRent111111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountAdminState extends string
        ? ReadonlyAccount<TAccountAdminState>
        : TAccountAdminState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountRent extends string
        ? ReadonlyAccount<TAccountRent>
        : TAccountRent,
      ...TRemainingAccounts,
    ]
  >;

export type ReallocMarketInstructionData = {
  discriminator: ReadonlyUint8Array;
  additionalBytes: bigint;
};

export type ReallocMarketInstructionDataArgs = {
  additionalBytes: number | bigint;
};

export function getReallocMarketInstructionDataEncoder(): FixedSizeEncoder<ReallocMarketInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 1)],
      ["additionalBytes", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REALLOC_MARKET_DISCRIMINATOR }),
  );
}

export function getReallocMarketInstructionDataDecoder(): FixedSizeDecoder<ReallocMarketInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 1)],
    ["additionalBytes", getU64Decoder()],
  ]);
}

export function getReallocMarketInstructionDataCodec(): FixedSizeCodec<
  ReallocMarketInstructionDataArgs,
  ReallocMarketInstructionData
> {
  return combineCodec(
    getReallocMarketInstructionDataEncoder(),
    getReallocMarketInstructionDataDecoder(),
  );
}

export type ReallocMarketInput<
  TAccountMarket extends string = string,
  TAccountSigner extends string = string,
  TAccountAdminState extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountRent extends string = string,
> = {
  market: Address<TAccountMarket>;
  signer: TransactionSigner<TAccountSigner>;
  adminState: Address<TAccountAdminState>;
  systemProgram?: Address<TAccountSystemProgram>;
  rent?: Address<TAccountRent>;
  additionalBytes: ReallocMarketInstructionDataArgs["additionalBytes"];
};

export function getReallocMarketInstruction<
  TAccountMarket extends string,
  TAccountSigner extends string,
  TAccountAdminState extends string,
  TAccountSystemProgram extends string,
  TAccountRent extends string,
  TProgramAddress extends Address = typeof EXPONENT_CORE_PROGRAM_ADDRESS,
>(
  input: ReallocMarketInput<
    TAccountMarket,
    TAccountSigner,
    TAccountAdminState,
    TAccountSystemProgram,
    TAccountRent
  >,
  config?: { programAddress?: TProgramAddress },
): ReallocMarketInstruction<
  TProgramAddress,
  TAccountMarket,
  TAccountSigner,
  TAccountAdminState,
  TAccountSystemProgram,
  TAccountRent
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? EXPONENT_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    market: { value: input.market ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: true },
    adminState: { value: input.adminState ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    rent: { value: input.rent ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.rent.value) {
    accounts.rent.value =
      "SysvarRent111111111111111111111111111111111" as Address<"SysvarRent111111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.adminState),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.rent),
    ],
    data: getReallocMarketInstructionDataEncoder().encode(
      args as ReallocMarketInstructionDataArgs,
    ),
    programAddress,
  } as ReallocMarketInstruction<
    TProgramAddress,
    TAccountMarket,
    TAccountSigner,
    TAccountAdminState,
    TAccountSystemProgram,
    TAccountRent
  >);
}

export type ParsedReallocMarketInstruction<
  TProgram extends string = typeof EXPONENT_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    market: TAccountMetas[0];
    signer: TAccountMetas[1];
    adminState: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
    rent: TAccountMetas[4];
  };
  data: ReallocMarketInstructionData;
};

export function parseReallocMarketInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedReallocMarketInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      market: getNextAccount(),
      signer: getNextAccount(),
      adminState: getNextAccount(),
      systemProgram: getNextAccount(),
      rent: getNextAccount(),
    },
    data: getReallocMarketInstructionDataDecoder().decode(instruction.data),
  };
}
